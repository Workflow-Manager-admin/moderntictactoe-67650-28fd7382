import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  output: "export",
  output: "export",
  output: "export",
  output: "export",
  output: "export",
  output: "export",
  output: "export",
  output: "export",
  output: "export",
  output: "export",
  output: "export",
  /* config options here */
};

export default nextConfig;
